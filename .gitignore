# Build artifacts
build/

# Node / package manager noise. The plugin itself has zero runtime deps;
# everything under node_modules/ is dev tooling (eslint, prettier, tsc,
# lefthook). The XPI build script never reads node_modules/.
node_modules/
npm-debug.log
yarn-error.log
pnpm-debug.log
.pnpm-store/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Local Zotero debug captures (handy during dev but not for the repo)
/tmp/zotero-snowball-*.log

# Local environment
.env
.env.local

# Local agent / editor state
.claude/
